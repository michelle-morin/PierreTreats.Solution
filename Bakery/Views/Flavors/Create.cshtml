@{
  Layout = "_Layout";
}

@model Bakery.Models.Flavor

<h1>ADD NEW FLAVOR</h1>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <br>
  <input type="submit" value="ADD THIS FLAVOR" class="btn-block" />
}